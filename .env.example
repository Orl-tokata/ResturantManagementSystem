# Copy to `.env` (gitignored). Used by docker-compose.yml.

# Password for the containerised PostgreSQL. Required — compose refuses to start without it.
DB_PASSWORD=change_me

# Optional: pgAdmin login, only used with `docker compose --profile tools up -d`
PGADMIN_PASSWORD=admin
